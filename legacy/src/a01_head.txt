<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
<title>Grass Painter</title>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Archivo:wdth,wght@75..125,400..700&family=IBM+Plex+Mono:wght@400;500&display=swap" />
<style>
/* ==========================================================================
   GRASS PAINTER — DESIGN TOKENS
   --------------------------------------------------------------------------
   Direction: "field station". The chrome is soil at dusk and the one accent
   is the sun, because green is the material the user paints — an interface
   that is also green competes with the work. Gold never occurs in a midday
   field, so it always reads as interface.
   ========================================================================== */
:root{
  /* --- the six --- */
  --soil:#16130E;      /* the void behind everything */
  --loam:#1F1B14;      /* chrome surfaces: bar, rail, panel */
  --husk:#2C271D;      /* raised things: inputs, tracks, buttons */
  --chalk:#F2EDE1;     /* primary text */
  --sun:#FFB43D;       /* the single accent */
  --clay:#EC7458;      /* destructive, and only destructive */

  /* --- derived --- */
  --husk-2:#3A3428;
  --straw:#A79B84;     /* secondary text */
  --straw-2:#948871;   /* tertiary text — lifted to clear 4.5:1 on --loam */
  --rule:#332C21;      /* hairlines */
  --rule-2:#443C2E;
  --sun-wash:rgba(255,180,61,.14);
  --sun-deep:#B57A1E;
  --moss:#8FBF52;      /* the one tie back to the content: grass readouts */

  /* --- aliases kept so inline styles elsewhere stay honest --- */
  --accent:var(--sun);
  --danger:var(--clay);

  /* radii — slightly softer than a pro DCC tool, this is for beginners */
  --r1:5px; --r2:9px; --r3:13px; --r4:18px;

  /* spacing */
  --s1:4px; --s2:8px; --s3:12px; --s4:16px; --s5:24px; --s6:34px;

  /* type */
  --f:"Archivo",-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;
  --fm:"IBM Plex Mono",ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
  --fs0:11px;    /* micro: unit labels, counts */
  --fs1:12px;    /* control labels */
  --fs2:13px;    /* body, buttons */
  --fs3:15px;    /* section titles */
  --fs4:19px;    /* panel + modal titles */
  --fs5:26px;    /* the rare big one */

  /* motion */
  --dur:140ms;
  --slow:260ms;
  --ease:cubic-bezier(.32,.72,.35,1);

  /* metrics */
  --rail:92px;
  --panelw:320px;
  --topbar:52px;
  --ctrlh:32px;
}

*{box-sizing:border-box;margin:0;padding:0}
html,body{height:100%;overflow:hidden}
body{
  background:var(--soil); color:var(--chalk);
  font-family:var(--f); font-size:var(--fs2); line-height:1.45;
  font-variation-settings:"wdth" 100;
  -webkit-font-smoothing:antialiased; text-rendering:optimizeLegibility;
  user-select:none; -webkit-user-select:none;
  overscroll-behavior:none;
}
button,input,select,textarea{font:inherit;color:inherit}
button{background:none;border:0;cursor:pointer;font-family:var(--f)}
::-webkit-scrollbar{width:10px;height:10px}
::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:var(--rule-2);border-radius:99px;border:3px solid var(--loam)}
::-webkit-scrollbar-thumb:hover{background:#554b39}
:focus{outline:none}
:focus-visible{outline:2px solid var(--sun);outline-offset:2px;border-radius:var(--r1)}

/* The wide grotesque names things: stations, sections, titles. Nothing else. */
.wide{font-variation-settings:"wdth" 118;letter-spacing:-.005em}
.num{font-family:var(--fm);font-variant-numeric:tabular-nums}

/* ==========================================================================
   LAYOUT
   ========================================================================== */
#app{position:fixed;inset:0;display:flex;flex-direction:column}

#topbar{
  height:var(--topbar); flex:0 0 auto;
  display:flex; align-items:center; gap:var(--s2);
  padding:0 var(--s3) 0 var(--s4);
  background:var(--loam); border-bottom:1px solid var(--rule);
  z-index:30;
}
.brand{display:flex;align-items:center;gap:9px;padding-right:var(--s2)}
.brand svg{width:22px;height:22px;color:var(--sun)}
.brand b{
  font-size:var(--fs3);font-weight:700;font-variation-settings:"wdth" 118;
  letter-spacing:.005em;white-space:nowrap;
}
.sep{width:1px;height:24px;background:var(--rule);flex:0 0 auto;margin:0 var(--s1)}
.grow{flex:1 1 auto}

#stage{position:relative;flex:1 1 auto;min-height:0}
#view{position:absolute;inset:0;width:100%;height:100%;display:block;touch-action:none;cursor:crosshair}
#view.cam{cursor:grab}
#view.cam.drag{cursor:grabbing}

/* ------- the settings panel ------- */
#panel{
  position:absolute; right:0; top:0; bottom:0; width:var(--panelw);
  background:var(--loam); border-left:1px solid var(--rule);
  display:flex; flex-direction:column; z-index:20;
  transition:transform var(--slow) var(--ease);
}
#panel.closed{transform:translateX(100%)}
#panel-head{
  flex:0 0 auto;padding:var(--s4) var(--s4) var(--s3);
  border-bottom:1px solid var(--rule);
}
#panel-title{
  font-size:var(--fs4);font-weight:700;font-variation-settings:"wdth" 118;
  line-height:1.15;display:block;
}
#panel-lede{
  display:block;margin-top:5px;font-size:var(--fs1);color:var(--straw);
  line-height:1.5;max-width:30ch;
}
#panel-lede b{color:var(--chalk);font-weight:600}
#panel-body{flex:1 1 auto;overflow-y:auto;overflow-x:hidden;padding:var(--s4) var(--s4) var(--s6)}

#panel-toggle{
  position:absolute;top:var(--s3);right:calc(var(--panelw) + var(--s3));
  width:30px;height:30px;border-radius:var(--r2);
  background:rgba(31,27,20,.9);border:1px solid var(--rule);color:var(--straw);
  display:flex;align-items:center;justify-content:center;z-index:21;
  backdrop-filter:blur(10px);-webkit-backdrop-filter:blur(10px);
  transition:right var(--slow) var(--ease),background var(--dur) var(--ease),color var(--dur) var(--ease);
}
#panel-toggle:hover{background:var(--husk);color:var(--chalk)}
#panel-toggle svg{width:16px;height:16px;transition:transform var(--slow) var(--ease)}
#panel.closed ~ #panel-toggle{right:var(--s3)}
#panel.closed ~ #panel-toggle svg{transform:rotate(180deg)}

/* ------- hint strip ------- */
#hint{
  position:absolute;left:50%;transform:translateX(-50%);bottom:var(--s4);
  display:flex;align-items:center;gap:7px;flex-wrap:wrap;justify-content:center;
  padding:9px 14px;border-radius:99px;
  background:rgba(22,19,14,.82);border:1px solid var(--rule);
  backdrop-filter:blur(12px);-webkit-backdrop-filter:blur(12px);
  font-size:var(--fs1);color:var(--straw);z-index:15;pointer-events:none;
  max-width:min(72vw,760px);
  transition:opacity var(--dur) var(--ease);
}
kbd{
  font:500 var(--fs0)/1 var(--fm);background:var(--husk);color:var(--chalk);
  border:1px solid var(--rule-2);border-bottom-width:2px;
  border-radius:var(--r1);padding:4px 6px;white-space:nowrap;
}

/* ==========================================================================
   CONTROLS — one vocabulary. Bigger targets than a pro tool would use,
   because the audience has never opened one.
   ========================================================================== */
.group{margin-bottom:var(--s5)}
.group:last-child{margin-bottom:0}
.group-title{
  display:flex;align-items:center;gap:var(--s2);
  font-size:var(--fs0);font-weight:600;font-variation-settings:"wdth" 118;
  letter-spacing:.1em;text-transform:uppercase;color:var(--straw-2);
  margin-bottom:var(--s3);
}
.group-title::after{content:"";flex:1 1 auto;height:1px;background:var(--rule)}

.ctrl{margin-bottom:var(--s3)}
.ctrl:last-child{margin-bottom:0}
.ctrl-top{display:flex;align-items:baseline;gap:var(--s2);margin-bottom:7px}
.ctrl-label{
  flex:1 1 auto;min-width:0;font-size:var(--fs1);color:var(--straw);font-weight:500;
  /* Labels wrap rather than truncate: a clipped label is a puzzle, and these
     are written as short sentences for people who have not met the term. */
  overflow-wrap:anywhere;
}
.ctrl-val{
  flex:0 0 auto;width:74px;height:22px;text-align:right;
  background:transparent;border:1px solid transparent;border-radius:var(--r1);
  font-family:var(--fm);font-size:var(--fs0);font-variant-numeric:tabular-nums;
  color:var(--chalk);padding:0 5px;
  transition:background var(--dur) var(--ease),border-color var(--dur) var(--ease);
}
.ctrl-val:hover{background:var(--husk);border-color:var(--rule-2)}
.ctrl-val:focus{background:var(--husk);border-color:var(--sun)}

/* slider — 20px tall hit area, 6px track */
.slider{position:relative;height:20px;display:flex;align-items:center;cursor:ew-resize;touch-action:none}
.slider .trk{position:absolute;left:0;right:0;height:6px;border-radius:99px;background:var(--husk)}
.slider .fill{position:absolute;left:0;height:6px;border-radius:99px;background:var(--sun-deep);
  transition:background var(--dur) var(--ease)}
.slider .knob{
  position:absolute;width:16px;height:16px;border-radius:99px;
  background:var(--chalk);border:3px solid var(--loam);
  transform:translateX(-50%);pointer-events:none;
  transition:background var(--dur) var(--ease),box-shadow var(--dur) var(--ease),transform var(--dur) var(--ease);
  box-shadow:0 0 0 0 var(--sun-wash);
}
.slider:hover .knob,.slider.act .knob{background:var(--sun);box-shadow:0 0 0 5px var(--sun-wash)}
.slider:hover .fill,.slider.act .fill{background:var(--sun)}
.slider.act .knob{transform:translateX(-50%) scale(1.12)}
.slider.fine .trk{background:var(--sun-deep);opacity:.45}

/* segmented */
.seg{display:flex;gap:3px;background:var(--husk);border-radius:var(--r2);padding:3px}
.seg button{
  flex:1 1 0;min-width:0;height:28px;border-radius:6px;font-size:var(--fs1);font-weight:600;
  color:var(--straw);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
  transition:background var(--dur) var(--ease),color var(--dur) var(--ease);
}
.seg button:hover{background:var(--husk-2);color:var(--chalk)}
.seg button[aria-pressed="true"]{background:var(--sun);color:var(--soil)}

/* chips — a row of named choices, used for landform and grass looks */
.chips{display:flex;flex-wrap:wrap;gap:6px}
.chip{
  height:30px;padding:0 11px;border-radius:99px;
  background:var(--husk);border:1px solid transparent;color:var(--straw);
  font-size:var(--fs1);font-weight:500;display:inline-flex;align-items:center;gap:7px;
  transition:background var(--dur) var(--ease),color var(--dur) var(--ease),border-color var(--dur) var(--ease);
}
.chip:hover{background:var(--husk-2);color:var(--chalk)}
.chip[aria-pressed="true"]{background:var(--sun-wash);border-color:var(--sun);color:var(--sun)}
.chip i{width:11px;height:11px;border-radius:99px;flex:0 0 auto;
  border:1px solid rgba(255,255,255,.16)}

/* generic button */
.btn{
  flex:0 0 auto;
  height:var(--ctrlh);padding:0 14px;border-radius:var(--r2);
  background:var(--husk);color:var(--chalk);
  font-size:var(--fs2);font-weight:500;display:inline-flex;align-items:center;
  justify-content:center;gap:8px;white-space:nowrap;
  transition:background var(--dur) var(--ease),color var(--dur) var(--ease);
}
.btn svg{width:16px;height:16px;flex:0 0 auto}
.btn:hover{background:var(--husk-2)}
.btn:active{transform:translateY(1px)}
.btn.icon{width:var(--ctrlh);padding:0}
.btn.ghost{background:transparent;color:var(--straw)}
.btn.ghost:hover{background:var(--husk);color:var(--chalk)}
.btn.pri{background:var(--sun);color:var(--soil);font-weight:600}
.btn.pri:hover{background:#FFC661}
.btn.dgr{color:var(--clay)}
.btn.dgr:hover{background:rgba(236,116,88,.16);color:var(--clay)}
.btn[disabled]{opacity:.35;pointer-events:none}
.btn-row{display:flex;gap:var(--s2);margin-top:var(--s3)}
.btn-row .btn{flex:1 1 0;min-width:0}

/* select */
.sel{position:relative;height:var(--ctrlh)}
.sel select{
  width:100%;height:100%;appearance:none;-webkit-appearance:none;
  background:var(--husk);border:0;border-radius:var(--r2);
  padding:0 30px 0 11px;font-size:var(--fs2);cursor:pointer;color:var(--chalk);
  transition:background var(--dur) var(--ease);
}
.sel select:hover{background:var(--husk-2)}
.sel select option{background:#241F17;color:var(--chalk)}
.sel svg{position:absolute;right:10px;top:50%;transform:translateY(-50%);
  width:14px;height:14px;color:var(--straw-2);pointer-events:none}

/* toggle */
.tgl{
  width:40px;height:23px;border-radius:99px;background:var(--husk);
  position:relative;flex:0 0 auto;
  transition:background var(--dur) var(--ease);
}
.tgl::after{
  content:"";position:absolute;left:3px;top:3px;width:17px;height:17px;border-radius:99px;
  background:var(--straw-2);transition:transform var(--dur) var(--ease),background var(--dur) var(--ease);
}
.tgl[aria-pressed="true"]{background:var(--sun-deep)}
.tgl[aria-pressed="true"]::after{transform:translateX(17px);background:var(--sun)}
.ctrl.row{display:flex;align-items:center;gap:var(--s3);min-height:26px}
.ctrl.row .ctrl-label{margin:0;white-space:normal}

/* colour */
.col{display:flex;align-items:center;gap:var(--s2)}
.col .sw{
  width:32px;height:var(--ctrlh);border-radius:var(--r2);
  overflow:hidden;position:relative;flex:0 0 auto;cursor:pointer;
  box-shadow:inset 0 0 0 1px rgba(255,255,255,.1);
}
.col .sw input{position:absolute;inset:-8px;width:calc(100% + 16px);height:calc(100% + 16px);
  border:0;padding:0;cursor:pointer;background:none}
.col .hex{
  flex:1 1 auto;height:var(--ctrlh);background:var(--husk);border:1px solid transparent;
  border-radius:var(--r2);padding:0 10px;font-family:var(--fm);font-size:var(--fs0);
  font-variant-numeric:tabular-nums;text-transform:uppercase;
  transition:border-color var(--dur) var(--ease),background var(--dur) var(--ease);
}
.col .hex:hover{background:var(--husk-2)}
.col .hex:focus{border-color:var(--sun)}

/* wind dial */
.dial{display:flex;align-items:center;gap:var(--s4);margin-bottom:var(--s3)}
.dial svg{width:70px;height:70px;flex:0 0 auto;cursor:grab;touch-action:none}
.dial svg:active{cursor:grabbing}
.dial .dial-rim{fill:var(--husk);stroke:none}
.dial .dial-tick{stroke:var(--rule-2);stroke-width:1.4}
.dial .dial-arrow{fill:var(--sun)}
.dial .dial-hub{fill:var(--loam);stroke:none}
.dial-info{flex:1 1 auto;min-width:0}

.note{font-size:var(--fs1);color:var(--straw-2);line-height:1.55;margin-bottom:var(--s3)}
.note:last-child{margin-bottom:0}
.note b{color:var(--straw);font-weight:600}
.meter{height:5px;border-radius:99px;background:var(--husk);overflow:hidden;margin-top:8px}
.meter i{display:block;height:100%;background:var(--moss);border-radius:99px;
  transition:width var(--slow) var(--ease),background var(--dur) var(--ease)}
.meter.warn i{background:var(--clay)}

/* the "everything else" disclosure that replaces the Simple/Advanced switch */
#more{
  margin-top:var(--s5);padding-top:var(--s4);border-top:1px solid var(--rule);
}
#more-btn{
  display:flex;align-items:center;gap:var(--s2);width:100%;
  font-size:var(--fs1);font-weight:600;color:var(--straw);
  transition:color var(--dur) var(--ease);
}
#more-btn:hover{color:var(--chalk)}
#more-btn svg{width:14px;height:14px;transition:transform var(--dur) var(--ease)}
#more.open #more-btn svg{transform:rotate(90deg)}
#more-body{display:none;padding-top:var(--s4)}
#more.open #more-body{display:block}

/* ==========================================================================
   OVERLAYS
   ========================================================================== */
#tip{
  position:fixed;z-index:200;pointer-events:none;max-width:250px;
  padding:8px 11px;border-radius:var(--r2);
  background:#332C21;color:var(--chalk);font-size:var(--fs1);line-height:1.5;
  box-shadow:0 10px 30px rgba(0,0,0,.55);
  opacity:0;transform:translateY(4px);
  transition:opacity var(--dur) var(--ease),transform var(--dur) var(--ease);
}
#tip.on{opacity:1;transform:translateY(0)}
#tip .k{color:var(--straw-2);margin-left:6px;font-family:var(--fm);font-size:var(--fs0)}

#toasts{position:fixed;left:50%;bottom:76px;transform:translateX(-50%);
  z-index:210;display:flex;flex-direction:column;gap:var(--s2);align-items:center;pointer-events:none}
.toast{
  padding:11px 18px;border-radius:99px;
  background:#332C21;color:var(--chalk);
  font-size:var(--fs2);box-shadow:0 12px 34px rgba(0,0,0,.6);
  animation:tin var(--slow) var(--ease);
}
.toast.err{background:#4A2A21;color:#FFC9BA}
.toast.ok{background:#3A3218;color:#FFD98A}
@keyframes tin{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}

.scrim{position:fixed;inset:0;z-index:150;background:rgba(10,8,5,.66);
  backdrop-filter:blur(5px);-webkit-backdrop-filter:blur(5px);
  display:flex;align-items:center;justify-content:center;padding:var(--s5);
  animation:fadein var(--dur) var(--ease)}
@keyframes fadein{from{opacity:0}to{opacity:1}}
.modal{
  width:min(620px,100%);max-height:84vh;overflow:auto;
  background:var(--loam);border-radius:var(--r4);
  box-shadow:0 30px 80px rgba(0,0,0,.6);
  animation:modin var(--slow) var(--ease);
}
@keyframes modin{from{opacity:0;transform:translateY(14px) scale(.985)}to{opacity:1;transform:none}}
.modal.wide-modal{width:min(880px,100%)}
.modal-head{display:flex;align-items:center;gap:var(--s3);padding:var(--s5) var(--s5) var(--s3)}
.modal-head b{font-size:var(--fs4);font-weight:700;font-variation-settings:"wdth" 118;flex:1 1 auto}
.modal-body{padding:0 var(--s5) var(--s5)}
.modal-body p{color:var(--straw);line-height:1.65;margin-bottom:var(--s4);max-width:64ch}
.keys{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:var(--s5)}
.keys h4{font-size:var(--fs0);letter-spacing:.1em;text-transform:uppercase;color:var(--straw-2);
  font-weight:600;font-variation-settings:"wdth" 118;margin:0 0 var(--s3)}
.keys dl{display:grid;grid-template-columns:auto minmax(0,1fr);gap:9px var(--s3);align-items:center}
.keys dt{justify-self:start;display:flex;gap:3px}
.keys dd{color:var(--straw);font-size:var(--fs1)}
.modal .btn-row{margin-top:var(--s5);justify-content:flex-end}
.modal .btn-row .btn{flex:0 0 auto;min-width:104px}

#nogl{position:fixed;inset:0;z-index:300;display:none;align-items:center;justify-content:center;
  padding:var(--s6);background:var(--soil);text-align:center}
#nogl .in{max-width:440px}
#nogl svg{width:42px;height:42px;color:var(--clay);margin-bottom:var(--s4)}
#nogl h2{font-size:22px;font-weight:700;font-variation-settings:"wdth" 118;margin-bottom:var(--s3)}
#nogl p{color:var(--straw);line-height:1.65;font-size:var(--fs2)}

#boot{position:fixed;inset:0;z-index:250;display:flex;align-items:center;justify-content:center;
  background:var(--soil);transition:opacity 320ms var(--ease)}
#boot.out{opacity:0;pointer-events:none}
#boot .in{display:flex;flex-direction:column;align-items:center;gap:var(--s4)}
#boot .bar{width:170px;height:3px;background:var(--husk);border-radius:99px;overflow:hidden}
#boot .bar i{display:block;width:38%;height:100%;background:var(--sun);border-radius:99px;
  animation:sweep 1.05s var(--ease) infinite}
@keyframes sweep{0%{transform:translateX(-100%)}100%{transform:translateX(365%)}}
#boot small{color:var(--straw-2);font-size:var(--fs0);letter-spacing:.1em;text-transform:uppercase;
  font-variation-settings:"wdth" 118;font-weight:600}

/* ==========================================================================
   RESPONSIVE
   ========================================================================== */
@media (max-width:1120px){
  #panel{box-shadow:-20px 0 50px rgba(0,0,0,.5);transform:translateX(100%)}
  #panel.open{transform:translateX(0)}
  #panel.closed{transform:translateX(100%)}
  #panel-toggle{right:var(--s3)}
  #panel.open ~ #panel-toggle{right:calc(var(--panelw) + var(--s3))}
  #panel.open ~ #panel-toggle svg{transform:rotate(0deg)}
  #panel.closed ~ #panel-toggle svg{transform:rotate(180deg)}
  .keys{grid-template-columns:1fr}
}
/* The top bar drops to icons before anything has a chance to squash. The
   wordmark and the two labelled buttons are the first things that can go —
   their icons already say what they do, and the tooltips still name them. */
@media (max-width:820px){
  .brand b{display:none}
  #saved span{display:none}
  #b-new span,#b-save span{display:none}
  #b-new,#b-save{width:var(--ctrlh);padding:0}
}
@media (max-width:760px){
  :root{--rail:66px;--panelw:min(340px,100vw)}
  #hint{display:none}
}

@media (prefers-reduced-motion:reduce){
  *{animation-duration:.001ms !important;animation-iteration-count:1 !important;
    transition-duration:.001ms !important}
}
</style>
